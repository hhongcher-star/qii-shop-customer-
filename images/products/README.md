# Product uploads

This directory contains runtime product uploads.

- Do not delete, replace, or mirror-clean this directory during deployment.
- Back up this directory separately from the application repository.
- The web-server user must have write permission on this directory.
- Only `.htaccess`, `.gitkeep`, and this file belong in Git.

